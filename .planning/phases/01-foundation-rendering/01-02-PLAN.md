---
phase: 01-foundation-rendering
plan: "02"
type: execute
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can type text in the chat input field and submit messages"
    - "Application loads in browser with modern SaaS aesthetic"
  artifacts:
    - path: "src/components/WhiteboardPanel.tsx"
      provides: "Diagram display area (placeholder for now)"
    - path: "src/components/Layout.tsx"
      provides: "Side-by-side resizable panels"
    - path: "src/App.tsx"
      provides: "Main application component"
  key_links:
    - from: "ChatPanel"
      to: "App"
      via: "React component"
    - from: "Layout"
      to: "ChatPanel, WhiteboardPanel"
      via: "children props"
---

<objective>
Build the chat panel with input field, message display, and the main layout with side-by-side panels. Implement the modern SaaS aesthetic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-rendering/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-rendering/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Layout component with side-by-side panels</name>
  <files>src/components/Layout.tsx</files>
  <action>
    Create Layout.tsx with:
    - Full viewport height container (h-screen)
    - Side-by-side flex layout on desktop (flex-row), vertical stack on mobile (flex-col)
    - Chat panel left (w-1/3 or flex-1), whiteboard right (w-2/3 or flex-2)
    - Draggable divider between panels (use simple mouse drag handler)
    - Minimal chrome, white background, subtle shadows
    - Responsive: stack vertically below md breakpoint
  </action>
  <verify>Layout renders two panels side-by-side on desktop</verify>
  <done>Layout component renders correctly with responsive behavior</done>
</task>

<task type="auto">
  <name>Create ChatPanel component with input</name>
  <files>src/components/ChatPanel.tsx</files>
  <action>
    Create ChatPanel.tsx with:
    - Message list area (scrollable, shows messages)
    - Input area at bottom with textarea and send button
    - White background, subtle border, modern typography
    - Indigo accent on send button (#6366f1)
    - Input auto-grows, max 4 lines
    - Submit on Enter (Shift+Enter for newline)
    - Welcome message state (shows when no messages)
    - Example prompts: "Create a sequence diagram for user login", "Make an ERD for an e-commerce app", "Draw a flowchart for order processing"
  </action>
  <verify>ChatPanel renders with input field and example prompts</verify>
  <done>ChatPanel component complete with input, messages display, and example prompts</done>
</task>

<task type="auto">
  <name>Create main App component</name>
  <files>src/App.tsx, src/main.tsx</files>
  <action>
    Create App.tsx importing Layout, ChatPanel, WhiteboardPanel
    Create main.tsx with React 18 createRoot rendering App
    Wrap in React.StrictMode
  </action>
  <verify>App renders without errors</verify>
  <done>Main application component tree complete</done>
</task>

</tasks>

<verification>
- [ ] Layout shows chat on left, whiteboard on right (desktop)
- [ ] Layout stacks vertically on mobile
- [ ] ChatPanel has working input field
- [ ] Example prompts display in welcome state
- [ ] Send button has indigo accent color
</verification>

<success_criteria>
Chat panel displays with input field, message area, and example prompts. Layout renders side-by-side panels on desktop with responsive mobile behavior. Modern SaaS aesthetic applied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-rendering/{phase}-02-SUMMARY.md`
</output>
